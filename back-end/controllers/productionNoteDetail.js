const { productionNoteDetailModel } = require("../controllers");

const productionNoteDetailController = {
    createProductionNoteDetail: (req, res) => {
        try {

        } catch(err) {
            return res.status(500).send("eroare");
        }
    }
}